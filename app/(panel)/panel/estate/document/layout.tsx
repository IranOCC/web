import { ReactNode } from "react";

export const metadata = {
  title: "انواع اسناد املاک",
};

const Layout = ({ children }: { children: ReactNode }) => {
  return children;
};

export default Layout;
