import { ReactNode } from "react";

export const metadata = {
  title: "ویژگی های املاک",
};

const Layout = ({ children }: { children: ReactNode }) => {
  return children;
};

export default Layout;
